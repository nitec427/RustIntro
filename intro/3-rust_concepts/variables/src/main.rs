/* global const value */
const THREE_HOURS_IN_SECONDS: u32 = 60 * 60 * 3;
fn main() {
    println!("In seconds {} ", THREE_HOURS_IN_SECONDS);

    // if you are going to change the variable type later on, you should declare them with just let without using mut. 
}
